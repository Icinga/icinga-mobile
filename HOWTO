Quick tips:

- Pinch the screen to search
- Swipe left to close the detail window
- Press the command button once to enable command mode, select the appropriate
host/service and hit the button again to send commands to these hosts/services

